import React from "react";
import { Icon } from "@iconify/react";

const ButtonSettings = () => {
  return (
    <button type="button" className="ButtonSettings">
      <Icon icon="codicon:three-bars" />
    </button>
  );
};

export default ButtonSettings;
